apiVersion: v1
kind: Service
metadata:
  name: no-as-a-service
  namespace: naas
spec:
  selector:
    app: no-as-a-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
